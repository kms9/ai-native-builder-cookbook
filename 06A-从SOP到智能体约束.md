---
title: "第 6A 章｜从 SOP 到智能体约束"
desc: "位于工作流重构与系统建设之间，把真实流程、SOP 和目标流程转换为任务、上下文、工具、权限、人审、异常、轨迹与评估的最小约束，并形成版本化转化卡。"
aliases:
  - "SOP到智能体约束"
  - "SOP转Agent规格"
  - "业务流程转系统契约"
tags:
  - "技术/AI/智能体"
  - "产品/SOP"
  - "产品/流程设计"
  - "学习/实践"
updated: 2026-08-05
---

# 第 6A 章｜从 SOP 到智能体约束

第 6 章已经完成两件事：先按最近一次任务还原现状，再重新设计人、AI 与确定性系统的目标工作流。澄川项目组没有批准一个开放式销售 Agent，只批准了一个范围有限、带人工确认和失败回退的 CRM 更新预览流程。

但目标流程仍然不能直接交给工程师开发。流程图中的“读取会议记录”“检索批准资料”“生成字段建议”“销售确认”和“写入 CRM”，都还只是业务语言。它们没有回答系统从哪里获得输入、哪些资料具有权威性、哪些动作允许自动执行、什么状态算完成，以及失败后怎样停止和恢复。

本章位于流程重构与系统建设之间，负责完成一次显式转换：

```text
真实任务
→ 现状流程
→ SOP
→ 目标流程
→ 智能体约束 V0
→ 第 7 章的系统契约
```

这里的“智能体约束”不是一段更长的提示词，也不是提前写完全部技术设计。它是一组跨业务、产品、工程、数据和风险角色都能检查的边界：任务何时开始、谁负责、允许读取什么、AI 做哪一步、哪些判断留给人、系统怎样记录、异常如何结束，以及后续用什么证据判断是否可构建和可试点。

## SOP 在这里解决什么问题

SOP 是 Standard Operating Procedure，通常译为标准作业程序或标准操作规程。它描述一项重复工作怎样在组织中被稳定执行。

一份可用于 AI 项目的 SOP，至少需要让另一名合格执行者知道：

1. 任务由什么事件或条件触发；
2. 谁发起、执行、审核并对结果负责；
3. 开始时需要哪些输入和业务状态；
4. 每一步做什么，在哪里发生判断、等待与交接；
5. 遇到缺失、冲突、权限不足或系统失败时怎么办；
6. 输出交给谁、怎样进入下一步并留下什么记录。

合格 SOP 的目标不是把所有工作变成僵硬步骤，而是把原本只存在于少数人经验中的稳定部分，转成团队可执行、可复查和可维护的过程。

### 六个容易混淆的对象

| 对象 | 主要回答的问题 | 典型内容 | 不能替代什么 |
|---|---|---|---|
| 流程图 | 工作如何在角色与系统间流转 | 步骤、角色、分支、系统 | 不能单独说明每一步的规则、异常和责任 |
| SOP | 一项任务如何稳定执行 | 触发、输入、步骤、判断、异常、输出、记录、版本 | 不能自动成为可运行系统 |
| Checklist | 执行前后需要检查什么 | 必做项、通过条件、责任人 | 不能替代完整流程和异常路径 |
| 模板 | 某类输出应该长什么样 | 字段、示例、反例 | 不能说明何时使用、依据什么生成 |
| Prompt / Instruction | 模型如何生成或判断 | 任务说明、上下文、格式、约束 | 不能替代权限、状态、工具和责任边界 |
| Agent / Workflow Spec | 系统如何运行目标流程 | 目标、输入、工具、状态、权限、人审、轨迹、评估 | 不能替代真实流程证据和业务责任 |

把 SOP 全文塞进 Prompt，不会自动得到可靠智能体。系统仍然不知道哪个数据源有效、哪个工具可以写入、人工确认怎样保存，以及一次工具超时后是否已经产生副作用。

## SOP-first 不是要求所有任务都固定化

“没有清晰 SOP，就不应该急着做智能体应用”适用于稳定、重复、可执行的业务流程，例如客服政策答复、合同初审、课程资源审核、销售拜访准备或会后 CRM 更新。

开放式研究、复杂诊断、创意探索和软件研发不一定能先写成固定 SOP。对这些任务，更重要的是先明确：

- Mission：要完成什么结果；
- Policy：允许与禁止的行为；
- Evidence Protocol：依据怎样获得和核验；
- Tool Boundary：可以调用哪些工具和数据；
- Stop Rule：什么情况下必须停止或转人工；
- Eval Contract：怎样判断结果和运行过程可接受。

因此，本书采用更精确的门槛：

> 对稳定业务流程，没有清晰 SOP，不进入智能体开发；对开放任务，没有明确目标、证据规则、工具边界、停止条件和责任主体，不进入高自主或生产部署。

## 先判断这条流程是否值得 SOP 化

不是所有流程都值得立刻标准化。第一个 SOP 智能体场景应优先选择高频、边界清楚、角色明确、规则可写、错误可发现且可以在两到四周内试点的任务。

| 判断维度 | 适合先 SOP 化 | 暂不适合立即 SOP 化 |
|---|---|---|
| 频率 | 每天或每周反复发生 | 一年只发生一两次 |
| 边界 | 触发、结束、输入、输出可以说明 | 目标和结束状态持续变化 |
| 角色 | 执行者、审核者和负责人明确 | 完全依赖某个人临场协调 |
| 规则 | 主要步骤和判断能够列出 | 主要依赖开放探索 |
| 风险 | 错误可以发现、拦截、回退或转人工 | 一次错误不可逆且无人负责 |
| 价值 | 能影响效率、质量、风险、体验或收入 | 只是“看起来适合 AI” |
| 试点 | 两到四周能观察真实或准真实任务 | 需要长期大集成后才知道是否有用 |

这里仍然不能只看总分。若没有业务负责人、没有可用样本，或高风险动作没有人工兜底，即使价值很高，也不应作为第一个建设切片。

## 从真实案例生成 SOP，而不是从理想流程开始

第 6 章已经强调：现状流程必须来自最近一次真实任务，而不是制度文件或管理者描述。SOP 的形成顺序也应保留这项证据纪律。

```text
选取最近一次任务
→ 按时间顺序复盘实际动作
→ 记录触发、输入、角色、系统、等待、返工、判断与输出
→ 比较多条任务，区分稳定部分与个别例外
→ 与制度、规则和负责人核对
→ 形成可执行 SOP 版本
```

现状事实、现行 SOP 和目标流程是三个不同对象：

| 对象 | 它说明什么 | 典型风险 |
|---|---|---|
| 现状事实 | 工作实际上怎样发生 | 样本过少，不能代表全部任务 |
| 现行 SOP | 组织要求工作怎样稳定执行 | 文档可能过期，或与现场脱节 |
| 目标流程 | 改造后希望任务怎样完成 | 可能只是设计假设，尚未经过真实使用 |

Builder 需要显式记录三者的差异。现场人员绕开 SOP，可能是执行偏差，也可能说明 SOP 不可用、系统不支持或组织激励不一致。不能在没有证据时把任何一方自动当作正确答案。

## SOP 先回答六个核心问题

把一份流程交给系统前，先检查它是否能回答下面六个问题。

| 核心问题 | 需要写清的内容 | 未写清时的系统风险 |
|---|---|---|
| 谁做 | 发起人、执行者、审核者、负责人 | AI、人和系统的责任混在一起 |
| 何时开始 | 事件、条件、不触发条件 | 系统误启动、重复启动或漏启动 |
| 输入什么 | 字段、文档、对象、来源、权限 | 上下文缺失、越权或读错对象 |
| 如何判断 | 规则、经验、授权和证据 | 模型把猜测写成业务结论 |
| 异常怎么办 | 缺失、冲突、失败、升级、暂停、回退 | 顺利路径能演示，真实任务无法结束 |
| 输出给谁 | 输出结构、下一步、记录与版本 | 生成结束了，业务状态却没有改变 |

若这六个问题仍然含糊，继续选择模型、搭知识库或接工具，只会把流程中的不确定性放大。

## 从 SOP 到智能体约束的映射

下面的映射表是本章的核心。左侧仍是业务流程，右侧开始进入系统设计。它不是要求每项都建设独立技术层，而是要求每个重要约束拥有明确落点。

| SOP 元素 | 在 Agent / Workflow 中的对应物 | 首轮需要回答的问题 |
|---|---|---|
| 触发条件 | 用户入口、事件、定时任务、Webhook 或人工启动 | 怎样避免重复触发和错误对象 |
| 角色 | 用户身份、审批人、负责人、交接对象 | 谁能读、写、确认和承担结果 |
| 输入 | 表单字段、业务对象、运行时上下文 | 必填项、来源、版本和权限是什么 |
| 步骤 | 工作流节点、任务状态、运行循环 | 哪些固定，哪些允许动态选择 |
| 判断规则 | 规则引擎、代码校验、Prompt、模型评审器 | 哪些必须确定性执行，哪些允许概率判断 |
| 知识资料 | RAG、知识库、版本化文档、案例库 | 什么来源可支持什么主张，冲突时怎么办 |
| 系统动作 | Tool、API、数据库或消息操作 | 调用身份、副作用、幂等和失败语义是什么 |
| 人审与异常 | 确认、驳回、修改、升级、降级、转人工和回退 | 谁接手、看什么、多久处理、怎样恢复 |
| 输出与记录 | 结构化输出、系统字段、Trace、审计日志 | 业务用户怎样使用，最终状态怎样核对 |
| 指标 | Eval Dataset、监控指标、Pilot 指标、发布门禁 | 什么叫答对、可用、安全和稳定 |

需要特别区分“提示约束”和“系统约束”。

| 约束类型 | 示例 | 主要执行位置 |
|---|---|---|
| 生成指导 | 输出必须区分客户原话与推断 | Prompt / Instruction |
| 结构约束 | 结果必须包含字段、来源和风险标记 | Schema / 代码校验 |
| 权限约束 | AI 不能修改价格和正式商机阶段 | 身份、字段白名单、服务端权限 |
| 状态约束 | 未经销售批准不能提交变更 | 状态机与业务不变量 |
| 副作用约束 | 写入超时后先查询状态，不生成新意图重试 | Tool Contract 与恢复协议 |
| 发布约束 | 高风险样本未全部通过时不得进入试点 | Eval 与 Release Gate |

提示词可以帮助模型遵守规则，但不能成为唯一控制边界。

## 形成智能体约束转化卡 V0

完成第 6 章的目标流程后，先生成一张 `SOP → Agent Constraint Card V0`。它是第 7 章的输入，不是一次性填完的项目总表。

### 01｜基本信息

| 字段 | 填写要求 |
|---|---|
| SOP 名称与编号 | 能稳定引用的名称和版本 |
| 业务角色与目标任务 | 谁在什么场景完成什么任务 |
| 触发与不触发条件 | 何时开始，什么情况不能启动 |
| 输入与来源 | 必要字段、文档、对象和来源 |
| 完成状态 | 业务状态怎样才算真正结束 |
| 当前证据等级 | P0—P4；事实、判断和假设分别标记 |

### 02｜流程与边界

| 字段 | 填写要求 |
|---|---|
| 现状流程摘要 | 来自观察的主要步骤、等待和返工 |
| 目标流程摘要 | 删除、标准化、确定性自动化和 AI 辅助后的路径 |
| AI 可做 | 具体到检索、提取、分类、草稿或建议 |
| AI 不做 | 最终承诺、高风险审批、越权读取或不可逆动作 |
| 人审节点 | 谁确认、看什么、允许什么操作 |
| 异常与回退 | 缺失、冲突、权限、工具失败和用户拒绝时怎样结束 |

### 03｜系统与治理假设

| 字段 | V0 阶段需要写到什么程度 |
|---|---|
| 工具与知识依赖 | 候选工具、来源和 Owner；尚未写完整 Schema |
| 权限与数据边界 | 最小必要范围和高风险禁区 |
| 输出结构 | 用户看到什么，系统准备写入什么 |
| 必留记录 | 来源、人工修改、确认、状态和错误 |
| 主要风险 | 越权、错误承诺、资料过期、重复副作用等 |
| 负责人 | 业务、内容、技术、风险和项目 Owner |

### 04｜评估与下一阶段

| 字段 | 填写要求 |
|---|---|
| 正常样本 | 至少一条能走完主路径的任务 |
| 边界或失败样本 | 至少一条能暴露异常和人审的任务 |
| 初步成功标准 | 答对、可用、安全、稳定分别如何观察 |
| G2 决定 | 继续、有条件继续、返工、暂停或终止 |
| 进入第 7 章的条件 | 哪些契约必须补齐，谁负责 |

完整空白模板见 [W06A｜SOP 到智能体约束转化卡](模板-W06A-SOP到智能体约束转化卡.md)。

## 转化卡不是静态表，而是版本化主干 Artifact

一张表如果试图在第 6 章结束时填完 Context、工具、权限、Eval、Pilot 和产品化，会迫使学习者编造尚未验证的内容。更可用的做法是让同一张卡沿 G2—G6 持续升级。

| 版本 | 形成阶段 | 主要新增内容 | 对应决定 |
|---|---|---|---|
| V0 业务约束 | 第 6A 章 | 目标、触发、输入、流程、AI 可做/不做、人审、异常 | G2：是否值得构建 |
| V1 系统约束 | 第 7 章 | Context、对象、工具、状态、权限、输出和 Trace | G3：边界是否可评估 |
| V2 验证约束 | 第 8 章 | Dataset、Rubric、失败分类、发布门禁和事件响应 | G4：是否可有限试点 |
| V3 试点约束 | 第 9 章 | 用户、任务范围、基线、指标、支持和退出条件 | G5：扩大、迭代或停止 |
| V4 资产约束 | 第 10 章 | Owner、版本、复用接口、维护、成本和退役 | G6：产品化、资产化或退役 |

版本升级时保留被否决方案和变更理由。下一版覆盖上一版内容时，也要能追溯是哪一条现场证据、失败记录或门禁决定促成修改。

## 澄川案例：把目标流程写成 V0 约束

下面的填写内容来自本书虚构案例，只展示方法，不代表真实企业证据。

### 基本信息

| 字段 | 澄川 V0 |
|---|---|
| SOP 名称 | 客户会议后 CRM 更新准备与确认 v0.1 |
| 业务角色与任务 | 大客户销售在会议结束后形成可检查的 CRM 更新预览，并决定是否提交 |
| 触发 | 已结束、已授权读取且能关联到沙盒商机的客户会议 |
| 不触发 | 无权限会议、未关联商机、正式环境、要求更新禁止字段 |
| 输入 | 会议转写、批准产品资料、沙盒商机当前值、销售审阅输入 |
| 完成状态 | 被批准字段已写入沙盒，系统重新查询并确认最终状态 |

### 流程与边界

| 字段 | 澄川 V0 |
|---|---|
| AI 可做 | 提取客户原话和候选行动项；检索批准资料；准备四类字段建议；标记不确定与冲突 |
| AI 不做 | 批准价格、交期、商机阶段；决定资料权威性；直接写正式 CRM；发送外部消息 |
| 人审 | 销售接受、修改、拒绝或升级建议；内容负责人处理资料冲突 |
| 确定性系统 | 关联对象、检查字段白名单、鉴权、生成差异、写入沙盒、查询结果和记录日志 |
| 异常 | 资料冲突阻塞受影响字段；CRM 版本变化后重新生成；写入结果未知时查询原请求，不创建新意图重试 |

### 初步验证

| 样本 | 预期行为 |
|---|---|
| 正常样本 | 四类字段生成带来源的建议，销售修改一项后确认，沙盒状态与预览一致 |
| 失败样本 | 两份批准资料冲突时停止相关字段、保留其他草稿并交给内容负责人 |
| G2 结果 | 有条件继续；仅建设固定受控工作流，不批准开放 Agent 和正式环境自动写入 |
| 第 7 章前置条件 | 补齐来源版本、业务对象、字段白名单、状态机、窄工具、幂等和运行轨迹 |

这张 V0 卡没有假装系统已经设计完成。它只把第 6 章已经得到支持的业务判断，转成第 7 章必须实现和验证的工程入口。

## 十个设计关注面，而不是十个物理技术层

一个 SOP 智能体应用通常需要同时考虑十个关注面：

1. 用户入口与触发；
2. 输入标准化；
3. Context 与证据；
4. Workflow / Agent 编排；
5. Tool 与系统动作；
6. 状态与恢复；
7. 人审与责任；
8. 安全、权限与数据边界；
9. Eval 与发布门禁；
10. Trace、监控与运营。

这些是设计关注面，不要求每个项目部署十个独立服务。一个轻量工作流可能在同一应用中实现输入、编排、状态和工具调用，但不能因为物理组件合并，就遗漏对应的责任和检查。

第 7 章将把这些关注面压缩为任务、上下文、对象、工具、状态和轨迹六层契约。两种结构的关系是：十个关注面用于检查是否遗漏，六层契约用于组织可运行系统。

## G2 到 G3 之间的交接门槛

第 6A 章不会新增一个独立 Gate。它把 G2 的业务决定整理成 G3 的系统输入。

进入第 7 章前，至少应满足：

- [ ] 现状流程来自实际任务或明确标记的模拟材料；
- [ ] 现行 SOP、现场事实和目标流程没有被混写；
- [ ] 任务触发、输入和完成状态可以说明；
- [ ] AI 可做与不可做已经具体到动作；
- [ ] 每个高风险动作都有明确人工责任；
- [ ] 至少一条异常能走到负责人、状态和恢复条件；
- [ ] 候选 Context、工具、输出和必留记录已经列出；
- [ ] 正常样本和失败样本已经保存；
- [ ] G2 决定包含范围、条件和下一负责人。

若卡片中大量出现“智能体自行判断”“需要时转人工”“系统自动处理”，说明边界仍然不可检查，应回到第 6 章返工。

## 常见错误

### 把制度或理想流程当成 SOP 事实

制度说明组织希望怎样工作，不能证明现场就是这样发生。先还原最近一次任务，再比较制度与实际差异。

### 把 SOP 全文变成一个 Prompt

Prompt 不拥有身份、权限、状态和外部副作用。需要把 SOP 拆到确定性规则、Context、Tool、人审、状态和 Eval 中。

### 只写 AI 可做，不写 AI 不做

能力清单会自然扩张。禁止动作、字段和环境需要进入服务端控制，而不只是页面说明。

### 写“低置信度转人工”但没有交接设计

人工接手需要接收角色、所需依据、等待状态、允许动作、超时规则和恢复条件。只有一句“转人工”，异常仍然没有结束。

### 把所有任务都升级为 Agent

步骤固定、工具有限和分支可列时，受控 Workflow 通常更容易检查、评估和维护。只有路径确实需要动态选择时，才升级局部自主性。

### 一次填完未来所有字段

V0 只记录当前证据支持的约束。Context、Tool、Eval 和 Pilot 的细节应在相应章节验证后升级，不得用想象填满表格。

## 你的练习

完成第 6 章的 W04—W06 后，使用 [W06A 转化卡](模板-W06A-SOP到智能体约束转化卡.md) 整理你的目标流程。

先写出任务触发、输入和真正完成状态。然后分别列出 AI 可做、AI 不做、人工决定和确定性系统动作。选择一条最可能发生的异常，写到明确负责人、状态、允许动作和恢复条件。

再把 SOP 元素映射到候选系统约束：哪些规则必须由代码执行，哪些需要 Context 来源与版本，哪些动作需要 Tool Contract，哪些判断需要人工确认，哪些失败应进入 Eval Dataset。

最后写下 G2 决定和进入第 7 章的前置条件。不要把未验证的工具、权限或收益写成已经确定的事实。

### 怎样判断答案是否合格

遮住系统名称和模型名称后，另一位读者仍能说明任务为什么开始、怎样结束、谁承担结果，以及哪类错误会阻止继续。

把 Prompt 假设为失效，再检查字段白名单、权限、状态和副作用是否仍然受控。若所有边界都依赖模型“听话”，转化卡不合格。

把正常路径移除，只看异常路径。若异常不能走到明确状态、负责人和恢复条件，仍需返工。

一份合格的 V0 卡可以直接交给业务、产品、工程和风险角色评审，并能列出第 7 章需要补齐的契约，而不是让工程师从流程图中自行猜测。

## 本章小结：先把流程写成约束，再把约束写成系统

SOP 的价值不是多写一份文档，而是把一项重复工作中的触发、输入、角色、判断、异常、输出和责任从个人经验中分离出来。

智能体应用也不是把 SOP 塞进模型。它需要把业务元素分别落到入口、Context、规则、对象、Tool、状态、人审、Trace 和 Eval 中，使系统能够执行、停止、核对和修复。

第 6 章回答“目标流程怎样工作”，本章回答“哪些边界必须先写清”，第 7 章才回答“这些边界怎样成为可运行契约”。

[上一章：重构人—AI—系统工作流](06-重构人AI系统工作流.md) ｜ [下一章：把业务变成可运行系统](07-把业务变成可运行系统.md)

## 本章来源与边界

本章由《SOP 梳理与智能体应用转化指南：为什么先写清流程，才谈智能体》及其配套 22 页图解整理，并结合本书现有的澄川案例、G0—G6 门禁、P0—P4 证据环境和第 7 章六层契约重新编排。

SOP、流程图、Checklist、模板和 Prompt 的区分属于本书用于企业 AI 项目的工作定义；不同组织可以使用其他名称。SOP-first 主要适用于稳定、重复、可执行的业务流程，不应被解释为所有开放式 Agent 任务都必须先写成固定步骤。
